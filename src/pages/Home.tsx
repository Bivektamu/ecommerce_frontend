import Hero from "../components/Hero"
import Grids from "../components/ui/Grids"

const Home = (props: Props) => {
  return (
    <>
      <Hero />
      <Grids>
        hellow world
      </Grids>
    </>
  )
}

export default Home